import { Routes } from '@angular/router';
import { DietinfoComponent } from './dietinfo/dietinfo.component';

export const routes: Routes = [
    {path:'app-dietinfo', component: DietinfoComponent}
];
